import { React, Fragment } from "react";
import { AppRouters } from "./components/routers/AppRouters";

export const App = () => {
  return (
    <Fragment>
      <AppRouters />
    </Fragment>
  );
};

export default App;
